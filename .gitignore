# Ignore all .env files anywhere (frontend + backend)
**/.env

# Ignore all node_modules folders anywhere
**/node_modules

# Gmail integration files (credentials + tokens)
# (removed - reverting to SendGrid webhooks)
